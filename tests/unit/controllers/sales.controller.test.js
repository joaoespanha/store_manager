const { expect } = require('chai');
const chai = require('chai');
const sinon = require('sinon');
const sinonChai = require('sinon-chai');

chai.use(sinonChai);

describe('CONTROLLER : tests POST /sales routes', () => {
  it('tests if it is possibel to register a new sale', async () => {
    
  })
})